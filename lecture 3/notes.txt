Aggregate Function:
   is a built in function in mySql/it return single row,value 
   Count();
   sum();
   max();
   min();
   Average();

Select:
    Use to filter the data

Group by Clause:
     use for grouping 

Logical Operators:
     And/Or/not 

Comparison operators:
   =,>=,<=
   

Filter Operators:
    in
    between
    Like   

Like operators:
    work as a regular expression/searching operators

% (wild card)    

